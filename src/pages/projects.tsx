import React, {FC} from 'react';
import NavBar from '../components/navbar';

const Projects: FC = () => {
    return(
        <div>
        <div>
            <h1>My Projects</h1>
        </div>
            <NavBar/>
        </div>
    )
};

export default Projects;